# Data-driven crypto discovery — 2026-09-18

## Changed objective
Ken explicitly asks to disregard the eight maxims and first analyse the actual data, formulate new hypotheses, then backtest. Existing eight-rule and low-frequency-screen conclusions remain historical records, not constraints on hypothesis generation. This module is separate and never rewrites their results. We retain BTCUSDT/ETHUSDT, Binance genuine spot minute bars, HK timezone, 2021-01-01 through 2026-09-14, no leverage/shorts/keys/live orders/paid service. The user has not authorized riskier instrument changes.

## Phase 1: data exploration BEFORE hypotheses
Only 2021-2023 labels may be inspected; warmup from 2020-10-01. Phase 1 downloader is hard-ended at 2024-01-01 HK time. Build fully observed hourly bars from minute OHLCV, retaining taker-buy quote volume. Inspect 13 feature families: returns 1/4/24/168/720h, volatility-normalized 4h shock, 24h vs 7d volatility, relative quote volume, taker-buy imbalance, close location, range position, VWAP displacement and cross-coin 24h relative move. Quantile groups and 20 joint states are explicitly exported for 4/24/72h subsequent executable returns. All inspected cells and years are retained in a trial register. Train-period quantiles are exploratory, not claimed out-of-sample.

All feature-event labels use actual next-hour+1 minute entry and future exit opens, with exact timestamps for exploration labels; incomplete future labels are disclosed/excluded only for event study. Events are decimated by holding horizon to reduce overlap, but still not independent; no in-sample p-value is treated as validation. Compare with unconditional same-calendar-month returns. Transaction costs are included in event returns. No phase-1 2024+ price data are used.

## Phase 2: freeze hypotheses, then test
After reviewing the phase-1 patterns, write HYPOTHESES.md and a machine-readable candidate configuration with up to four primary hypotheses, exact entry rule, holding/exit, threshold origin, evidence and disconfirming risks. Freeze before loading 2024+ data in this module. Test independently capitalized 100% spot/cash accounts, one position at a time; never add returns across accounts. Initial/final costs included. Signals are hourly completed bars, orders no earlier than the following boundary+1 minute. No take-profit or stop values tuned from later results. Missing future prices cannot veto an entry in the backtest: use first real subsequent price for a due exit, and report delay/gap exposure.

Year 2024 is a fixed-rule validation step; 2025-2026 is a later audit. The entire 2021-2026 history has been examined in earlier project rounds, so neither is a genuinely new blind holdout; explicitly label reused history. Do not revise hypotheses after their 2024/2025-2026 performance. Report all primary rules, coins, years and failures. Any adaptive future model requires an explicitly purged, rolling training design; this round uses frozen rules and thresholds.

Controls: cash, buy-and-hold, and unconditional entry using the same clock/holding/exit; add fixed-seed, within-year whole-day-shifted signal controls. Report compound returns, completed trades, win rate, mean/median trade returns, PF, exposure, minute-observable max drawdown, and daily dependence-aware uncertainty. Base costs per side: fee .001, adverse slip .0005. Stress .0015/.0015; zero-cost also reported. Threshold and holding perturbations must be symmetric (0.75/1.0/1.25), all shown rather than choosing best cells. Multiple testing must include all primary hypotheses x coins x inferential comparisons; all discovery trials disclosed, no claim that a small final test family erases exploration bias. Bootstrap daily blocks 7/28 days as primary/diagnostic, fixed seed, minimum 4999 repetitions.

Success means executable positive net performance with stable controls and later-period/parameter evidence, not the ability to find one positive backtest. A promising historical candidate is not approved for live money or guaranteed profitable. A hypothesis which fails still yields a completed experiment; never tune until profitable.

## Data integrity and execution
Verify every official archive SHA-256 CHECKSUM; store URL, digest and timestamps. Detect ms/us timestamps, invalid OHLCV, duplicate/conflicting minutes, missing intervals and anomalous legacy close metadata. Minute identity is open_time; abnormal close metadata is logged, never silently edited. No invented or forward-filled executable prices. Indicators require their actual historical inputs. Incomplete hourly bars are not valid signal inputs. Intraminute ordering, minimum notional, order-book liquidity, size impact, exchange and USDT risks are not fully modeled. Official fees serve only as a plausible fixed model, not reconstructed personal historical fees.

Sources: Binance official public-data documentation https://github.com/binance/binance-public-data ; current general spot fee schedule https://www.binance.com/en/fee/trading . The hypotheses will be based on the measured training data rather than importing claimed profitability from any paper or social-media rule.

Code, tests and research status: GitHub. Decisions and concise status: existing Notion Incubator. Files returned in this conversation; no new Drive destination assumed. Standard public GitHub runner, no raw archive artifacts or ongoing scheduled service.
