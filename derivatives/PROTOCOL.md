# Derivatives information increment — frozen 2026-09-19

## Goal and boundaries
Continue Ken's approved precursor research by obtaining genuinely new information, not stacking more price indicators. Existing mother repository, original BTCUSDT/ETHUSDT spot outcomes, no live trading, shorting, leverage, new paid service or automatic merge. Funding/open-interest describe futures participants but are predictors of SPOT returns, not simulated futures funding income. Preserve prior results and PR #4 review findings.

## Collection and coverage gates
Official Binance USD-M futures historical fundingRate and daily metrics archives, 2023-12 warmup through 2026-09-14. Validate official CHECKSUM, retain each URL/hash/status, numeric schema and UTC timestamps, reject conflicting duplicates. Report missing files/rows and period coverage; do not fabricate. Probe GDELT historical article windows (2024,2025,2026) and Farside ETF tables for feasible timestamped historical news/flows. Current revised ETF tables without original publication/version timestamps do not qualify as predictive point-in-time data. No-news and unavailable-news are distinct.

Reuse hash-verified original spot hourly inputs and FOMC records from the previous conversation package, tied to archive run35336304622. This is inherited minute-data audit, not a new spot download. Freeze code/input/output manifests. No minute OHLCV blobs committed to main. Preserve compact research evidence in the research branch; publish a rerunnable collection command and completion receipt.

## Time alignment
Use settled funding values only after a conservative 1-hour availability delay; use metrics observations only after 1 hour as well. This is an explicitly assumed historical availability lag: archive timestamp is NOT evidence of original first-publication or absence of revisions. Additional 6-hour lag is a predeclared stress test, not best-lag selection. Backward-only as-of join; metric observations expire after 2 hours beyond the assumed lag, funding after 12 hours beyond lag. Never backfill. Infer/retain actual funding intervals; no permanent 8-hour assumption. Missing required values exclude a timestamp from both baseline and augmented model so comparisons use identical cases; original all-date baseline also reported separately. Require >=90% common hourly coverage in EACH year/coin for full coverage label. Lesser coverage is limited-period research, not full-window completion.

## Feature increment
Original 76 inputs preserved. New funding family: last settled rate normalized to rate per hour, its 3-settlement mean, 3-settlement change, settlement age. Open-interest family: log changes of base-unit OI over 1/6/24 hours and value-unit OI over 24h (reported as mixed price/quantity, not pure leverage). Positioning family: log top-account long/short, top-position long/short, all-account long/short, taker buy/sell ratio, plus their 6h changes. Absolute OI levels and raw dates excluded. New feature definitions only use causal past observations; train-only medians where needed, no future-data imputation. Drop an entire family only for unavailable source fields, explicitly record deviation, not performance-based selection.

## Identical-date experiment, frozen before augmented results
Same labels: next genuine minute open to 24h-later minute open, >=+1%, <=-1%, neutral. Same 2024 training / 2025 screening / reused 2026 audit through Sep14, with 25h purge at 2024/2025 ends. Historical data have been viewed repeatedly; NOT blind OOS and not forward evidence.

Primary model: unchanged HistGradientBoostingClassifier(max_iter=100,learning_rate=.05,max_leaf_nodes=7,max_depth=3,min_samples_leaf=168,l2_regularization=10,early_stopping=False,random_state=20260919). Fit B=original76, F=B+funding, O=B+OI, P=B+positioning, A=all. All compare to B and unconditional train frequencies on the SAME rows. Fixed logistic C=.1 and depth4/max10leaf/min168 tree: B versus A, diagnostic; no hyperparameter sweep. Report log loss, multiclass Brier, up/down AUC and calibration. 2026 mean per-observation log-loss reduction: bootstrap 7-day blocks (1999, seed20260919), 8 comparisons (4 increments x 2 coins) Holm family; 28-day block diagnostic. Score improvement is NOT trading profitability.

## Interpretable patterns and selection
Train one augmented tree per coin on 2024. Extract all leaf conditions. Reuse previous matched-control method (quarter x 30-day direction x 2024 volatility tercile; >=30 control hours), all up/down patterns and nonoverlapping24h counts. BEFORE 2026 rule outcomes, select at most one up and one down per coin from 2025: >=30 nonoverlap cases, >=5pp matched improvement, >=70% coverage, >=3 positive quarters, positive 2024 lift; up also positive mean net return at base costs. 2026 audit: >=30 nonoverlap cases, >=70% matching, >=2 positive quarters, corrected p<.05 with all leaf x direction tests retained; 28-day diagnostic. Add direct regression tests for weekly cluster bootstrap and eligibility before reuse (PR #4 finding).

Only selected up rules receive a diagnostic spot/cash 24h account, single position, next minute execution, no same-minute reentry. Costs per side base .10% fee+.05%slippage, stress .15%+.15%. No new stop/target or profit-based tuning. Record net return, trade count/win rate/mean/median/PF, hourly-observable drawdown, best-trade concentration. Down rules are risk alerts, not unauthorized shorts.

## Acceptance and disclosure
Actual source coverage table; model increment table; complete pattern/counterexample tables; any selected-rule returns; time leakage and bootstrap regression tests; executed notebook hash bound to receipt; reproducible source, exact inputs and compact outputs. New archival data lacking certified historic availability retain a replay limitation, even if delayed models improve. If no candidate survives, report no promotion without claiming all derivatives/news are useless. A failed source probe is an access result, not proof that no such data exist.

Official sources: https://github.com/binance/binance-public-data ; https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/rest-api/market-data ; https://blog.gdeltproject.org/gdelt-doc-2-0-api-debuts/ ; https://farside.co.uk/bitcoin-etf-flow-all-data/ .
